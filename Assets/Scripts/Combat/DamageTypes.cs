using UnityEngine;

public enum DamageType
{
    None,       // No damage type
    Physical,   // ⚔️ Base scaling from equipment stats & passives
    Fire,       // 🔥 Base scaling from equipment stats & passives
    Cold,       // ❄️ Base scaling from equipment stats & passives
    Lightning,  // ⚡ Base scaling from equipment stats & passives
    Chaos       // ☠️ Base scaling from equipment stats & passives
}
